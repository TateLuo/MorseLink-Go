本目录存放项目私有模块（PlatformIO 会自动扫描并参与编译）。

当前模块分两类：

1. 已有实现
- `BatteryMonitor`
- `Buzzer`
- `Common`
- `ConfigManager`
- `DisplayDriver`
- `InputManager`
- `JwtUtil`
- `LedManager`
- `LogManager`
- `MqttManager`
- `NetManager`
- `UI`
- `UIController`

2. 预留空模块
- `AudioManager`
- `BoardSupport`
- `CWEngine`

建议规范：
- 每个模块保持 `src/` 子目录，包含 `.h/.c/.cpp`
- 模块间通过头文件接口通信，避免直接依赖实现细节
- 新增模块时同步更新 `docs/目录结构.md` 与 `docs/模块说明.md`
